# 개발 인수인계 규칙

## 먼저 읽기

1. `README.md`: 실행 명령과 현재 범위.
2. `docs/handoff.md`: 구현된 것, 미구현 항목, 다음 작업과 수용 기준.
3. 해당 기능의 현행 설계: `docs/combat-rules.md`, `docs/vertical-slice.md`, `docs/web-ux-tech.md`.

v0.1 원안은 보관 자료이며 현재 구현 기준이 아니다. 설계와 코드가 다르면 현재 차이를 인수인계 문서에 명시하고, 작업 범위 내에서 정합성을 맞춘다.

## 구조

- 게임 규칙·명령 검증·피해 예측은 `packages/core`만 담당한다. DOM, Phaser, React, 저장소, 시계, 네트워크, `Math.random`을 참조하지 않는다.
- `packages/schema`는 데이터 타입·검증, `packages/content/data`의 JSON은 콘텐츠 원본이다. 코어에 시나리오 좌표나 캐릭터 ID를 하드코딩하지 않는다.
- `apps/web/src/game`은 Phaser 렌더링, `ui`는 React UI다. 입력을 코어 명령으로 바꾸고 결과를 표시한다. 화면에서 피해·이동 가능 여부를 따로 계산하지 않는다.
- 코어 상태는 직렬화 가능해야 한다. 임시 이동·선택은 UI 상태다. 실패한 명령은 원래 상태와 로그를 바꾸지 않는다.
- UI 표시용 임시 그림은 코드로 그린 픽셀 도형이다. 외부 에셋 도입 시 출처와 사용 조건을 기록한다.

## 개발과 검증

- Node 24.x, pnpm 11.1.2, lockfile을 사용한다. 최초 실행은 `pnpm install --frozen-lockfile`.
- 규칙 변경에는 문서 수용 사례와 연결되는 테스트를 추가한다. 함수 구현을 그대로 복사하는 테스트보다 경계·예외·재현성을 확인한다.
- 완료 전 `pnpm check`, `pnpm build` 실행. 화면·입력 변경은 `pnpm test:e2e`와 실제 화면 확인도 수행한다.
- 의존성 경계 검사는 간단한 정적 검사이므로 코드 리뷰를 대신하지 않는다.
- `docs/handoff.md`에 완료 범위·남은 작업·실행한 검증을 갱신한다. 미구현 UI를 동작하는 것처럼 표시하지 않는다.
- 다음 작업은 작은 독립 기능 단위로 진행한다. 캠페인·온라인·100직업을 한 번에 확장하지 않는다.
- 사용자 변경을 보존하고, 명시된 작업 범위 안의 가역적 구현은 자율적으로 진행한다. 인증이 필요하면 토큰을 파일·대화·로그에 노출하지 않는다.
